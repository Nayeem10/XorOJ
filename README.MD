*** Backend requirements ***
1. Install PostgreSQL
2. Set environment variable. To do this create a .env file in the same directory as application.yml (username, database name, password) 

*** Frontend ***

1. Run the following command inside frontend directory

```
npm install

```



